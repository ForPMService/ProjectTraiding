# Prometheus — scrape config для Docker Compose.
# Collector открывает prometheus exporter на порту 8889.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    scrape_interval: 15s
    static_configs:
      - targets: ['otel-collector:8889']
